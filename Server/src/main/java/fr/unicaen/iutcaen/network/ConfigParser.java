package fr.unicaen.iutcaen.network;

import java.util.Properties;
import java.io.InputStream;

public class ConfigParser {
    private Properties properties;

    /**
     * Loads the config file into a Properties object
     * @param configFile
     */
    public ConfigParser(String configFile) {
        // Charger le fichier de configuration dans un objet Properties
    }

    // Retourne la valeur d'une propriété à partir d'une clé

    /**
     * Returns the value of a property from a key
     * @param key
     * @return the value of a property
     */
    public String getProperty(String key) {
        return "";
    }
}
